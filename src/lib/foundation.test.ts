import { describe, expect, it } from 'vitest'

describe('project foundation', () => {
  it('has the Portal Warga project identity', () => {
    expect('portal-warga-rw').toBe('portal-warga-rw')
  })
})
