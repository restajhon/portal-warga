'use client'
import Link from 'next/link'
import {FormEvent,useState} from 'react'
import {signIn} from 'next-auth/react'
import {loginSchema} from '@/lib/auth/login-schema'
export default function AdminLoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');const parsed=loginSchema.safeParse({email,password});if(!parsed.success){setError(parsed.error.issues[0]?.message??'Periksa isian login.');return}setLoading(true);const result=await signIn('credentials',{email:parsed.data.email,password:parsed.data.password,portal:'ADMIN',redirect:false});if(result?.error){setError('Akun pengurus tidak ditemukan, belum aktif, atau tidak memiliki akses.');setLoading(false);return}window.location.assign('/cms')}return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-[#0B102F] p-12 text-white lg:flex lg:flex-col lg:justify-between"><Link href="/admin/login" className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-[10px] bg-[#5B4BFF] text-xs font-extrabold">RW</span><span className="font-bold">CMS Pengurus RW</span></Link><div><p className="text-[11px] font-bold tracking-[1.6px] text-[#AAA5FF]">AREA INTERNAL</p><h1 className="mt-5 text-4xl font-bold leading-tight">Kelola lingkungan dengan lebih terarah.</h1><p className="mt-5 text-sm leading-6 text-[#C7C9D6]">Publikasikan informasi, proses laporan warga, dan jaga transparansi RW 08.</p></div><p className="text-xs text-[#9196AD]">Akses khusus pengurus terverifikasi.</p></section><section className="flex items-center justify-center bg-[#F5F6FA] px-6 py-10 sm:px-12"><div className="w-full max-w-[430px] rounded-[22px] bg-white p-8 sm:p-10"><p className="text-[11px] font-bold tracking-[1.5px] text-[#5B4BFF]">PORTAL PENGURUS</p><h2 className="mt-3 text-3xl font-bold text-[#111322]">Masuk ke CMS RW</h2><p className="mt-3 text-sm leading-6 text-[#6F7385]">Gunakan akun pengurus yang telah diaktifkan oleh Super Admin.</p><form onSubmit={submit} className="mt-8 space-y-5"><Field id="admin-email" label="Email pengurus" type="email" value={email} onChange={setEmail} disabled={loading}/><Field id="admin-password" label="Password" type="password" value={password} onChange={setPassword} disabled={loading}/>{error&&<p className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700" role="alert">{error}</p>}<button disabled={loading} className="h-12 w-full rounded-full bg-[#5B4BFF] text-sm font-bold text-white disabled:opacity-60">{loading?'Memproses...':'Masuk ke CMS'}</button></form><p className="mt-6 text-center text-sm text-[#6F7385]">Warga? Gunakan <Link className="font-bold text-[#5B4BFF]" href="/">Portal Warga</Link></p></div></section></main>}
function Field({id,label,type,value,onChange,disabled}:{id:string;label:string;type:string;value:string;onChange:(v:string)=>void;disabled:boolean}){return <div><label className="mb-2 block text-xs font-bold text-[#111322]" htmlFor={id}>{label}</label><input id={id} name={id} type={type} value={value} onChange={e=>onChange(e.target.value)} disabled={disabled} className="h-[54px] w-full rounded-xl border border-[#E7E8EF] bg-[#F8F8FB] px-4 text-sm text-[#111322] outline-none focus:border-[#5B4BFF] focus:ring-4 focus:ring-[#5B4BFF]/10"/></div>}
